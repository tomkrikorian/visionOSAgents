# SharePlay Spatial API

Use this file as the official API map for GroupActivities features on visionOS.
Load the workflow-specific reference instead of repeating implementation rules
here.

## API Routing

- Activation and invitation APIs: load
  [`activation-ui.md`](activation-ui.md).
- `GroupSession` lifecycle and `groupActivityAssociation(_:)`: load
  [`group-session-lifecycle.md`](group-session-lifecycle.md).
- `GroupSessionMessenger` and `GroupSessionJournal`: load
  [`messaging-journal.md`](messaging-journal.md).
- `SystemCoordinator`, group immersive spaces, spatial templates, and group
  immersion style: load [`spatial-coordination.md`](spatial-coordination.md).
- Launch-only immersive-space recipes: load
  [`visionos-immersive-space.md`](visionos-immersive-space.md).
